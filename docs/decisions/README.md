# Architecture decision records

One file per decision that would be expensive to reverse. Numbered sequentially.
Once merged, an ADR is immutable - a changed mind gets a new ADR that supersedes
the old one, and the old one gets a "Superseded by NNNN" line.

Format: Context, Decision, Consequences, Status.
