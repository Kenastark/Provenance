# Demo materials

**Current versions (use these).**

- `demo-script-v1.2-descriptor-rename.md` — the 7-minute script. Supersedes v1.1-real-data.
- `judge-questions-v1.1-real-data.md` — prepared answers. Supersedes v1.0.
- `one-page-description-v1.2-descriptor-rename.md` — supersedes v1.1-real-data.
- `video-storyboard-v1.2-descriptor-rename.md` — supersedes v1.1-real-data.
- `checkpoint-3-capture-checklist-v1.0.md` — still current; carries no data figures.

The v1.0 and v1.1-real-data files for the three renamed documents are kept unedited per
standing rule 10 and must not be read from on stage.

**Why v1.0 must not be used.** Those files were written before the real export was
loaded. They quote a completeness figure that came from the synthetic test corpus, and —
more seriously — the script and storyboard state the **wrong verdict** for the KER11
event (AMBIGUOUS, routed to review; the real drop returns LIKELY_FAULT and does not route
to review). Each v1.1 file opens with a table of exactly what changed and why. Derivation:
`docs/updates/u22-headline-reconciliation.md`.

**Why v1.1-real-data was superseded (for the three above).** Only the product descriptor
changed — "AI Trust Layer for Environmental Data" → "An AI trust layer for Environmental
Sensor Networks.", matching `CLAUDE.md`'s updated definition — not any figure or verdict.
v1.1-real-data's numbers are still the correct numbers; only v1.0's are wrong. Each v1.2
file's own header says so explicitly.

The demo runs offline. Conference wifi fails; assume it.
