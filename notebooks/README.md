# Notebooks

Exploratory only. Nothing in `src/` may ever import from here — an architecture
test enforces it.

If something in a notebook turns out to be useful, move it into `src/provenance`
with tests. A notebook is a place to look at data, not a place to keep logic.
