# Scripts

| Script | What it does |
|---|---|
| `setup-github.sh` | Creates the GitHub repo, labels, milestones, and branch protection. Run once. |

Phase 7 adds `record-demo.sh` for the fallback demo recording.
